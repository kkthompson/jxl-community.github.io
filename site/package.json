{
  "name": "jpegxl-info-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "rm -rf dist && ASTRO_TELEMETRY_DISABLED=1 astro build && mkdir -p dist/old && cp -Rv public/old/art dist/old/",
    "preview": "ASTRO_TELEMETRY_DISABLED=1 astro preview"
  },
  "dependencies": {
    "astro": "^6.1.9"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
